#include <MOHPC/Network/Types/UserButtons.h>

using namespace MOHPC;
using namespace MOHPC::Network;

const userButton_t UserButtons::AttackPrimary = (1 << 0);
const userButton_t UserButtons::AttackSecondary = (1 << 1);
const userButton_t UserButtons::Run = (1 << 2);
const userButton_t UserButtons::Use = (1 << 3);
const userButton_t UserButtons::LeanLeft = (1 << 4);
const userButton_t UserButtons::LeanRight = (1 << 5);
const userButton_t UserButtons::InMenu = (1 << 6);
