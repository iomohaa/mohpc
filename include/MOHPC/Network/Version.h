#pragma once

#include "NetGlobal.h"

namespace MOHPC
{
	namespace Network
	{
		/**
		 * The string represent a decorative version number.
		 * It is not necessary to use this unless to tell
		 * the client/server about the version (in userinfo).
		 */
		extern MOHPC_NET_EXPORTS const char NETWORK_VERSION[];
	}
}
